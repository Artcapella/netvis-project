{
  "buildCommand": "cd explorer && npm install && npm run build",
  "outputDirectory": "explorer/dist",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
