{
  "buildCommand": "npm --prefix explorer run build",
  "outputDirectory": "explorer/dist",
  "installCommand": "npm --prefix explorer install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
